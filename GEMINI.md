<!-- GENERATED BY `hatch compile` — DO NOT EDIT.
     Source of truth: .hatch/{charter.md,roles/,registry.yaml}.
     Edit the SSOT then re-run `hatch compile`. -->

# Hatch — Agent Operating Context

_Surface: GEMINI.md (Antigravity / agy) · read by: agy_

## Mission (L0)

# Charter (L0 — mission)

> Đây là tầng L0: ngắn gọn, mọi agent nạp mỗi message. Giữ dưới ~300 token.
> Sửa file này là sửa SSOT — chạy `hatch compile` để đẩy xuống mọi agent.

## Sản phẩm
**Hatch** — embedded harness cho một squad coding-agent làm chung trên một repo.
Coding agent là entrypoint và **tự lái**; Hatch cung cấp **chat dùng chung**
(= giao tiếp + backlog, mỗi thread = một task) và **Knowledge Base**, phơi cho
agent qua một **MCP server**. CLI Go single-binary; dùng được với Claude Code,
Codex, Antigravity (`agy`), Kiro.

## Nguyên tắc tối cao
- **Embedded, không điều khiển**: Hatch KHÔNG spawn/lái agent. `board`/`chat`/
  `status` chỉ là view read-only. Quy trình là **protocol compile thành prose**
  (CLAUDE.md/AGENTS.md/…), không phải engine cưỡng chế.
- **SSOT → compile**: sửa `.hatch/{charter,roles,registry,workflow}` rồi
  `hatch compile`; không sửa file output.
- **Minimum code, surgical**: giải đúng vấn đề, không abstraction thừa. Lean
  Hexagonal (model / port / adapter).
- Mỗi thay đổi có dấu vết (ledger) + lý do (`why`); không sửa ngoài scope ticket.

## Ràng buộc
- Go 1.24+. Build sạch **cả hai**: mặc định và `-tags hatch_legacy`.
- **Không sửa file ngoài repo**: config home của Codex/agy do `hatch setup` lo;
  `hatch doctor` chỉ gọi lệnh auth của CLI, KHÔNG quét thư mục creds.
- `make lint && make test` (+ legacy) phải pass trước khi push; `go mod tidy` nếu đổi deps.

## Your roles (L1)

Bạn có thể giữ các vai dưới đây. **Vai hiện hành tùy task (thread) bạn nhận trong chat** — đọc thread để biết mình đang đóng vai gì.

### Role: Implementer (`implementer`)

## Trách nhiệm
- Viết code đúng scope ticket, đạt Definition of Done.
- Viết/chỉnh test liên quan; giữ lint sạch.

## Ranh giới (KHÔNG)
- Đổi scope ticket giữa chừng (cập nhật ticket trước).
- Sửa file ngoài `context_refs` đã khai.

## Cách làm
- Claim ticket ⇒ đọc `context_refs` + KB liên quan trước khi code.
- Xong ⇒ ghi handoff note + đẩy sang `review/`.
- Gặp gotcha đáng giữ ⇒ ghi `kb/learnings/`.

### Role: Tech Writer (`tech-writer`)

## Trách nhiệm
- Docs, changelog, runbook đi kèm thay đổi.

## Ranh giới (KHÔNG)
- Đổi hành vi code.

## Cách làm
- Bám `context/product`; viết cho người đọc thật, không lặp lại code.
- Cập nhật KB/index khi thêm tri thức tra cứu.

## Workflow — scrum (mô tả, không phải engine)

Đây là quy trình squad **tự tuân theo** qua chat. Trạng thái task suy ra từ hội thoại trong thread (post `done`/`block`/`decision`), không cần lane-engine.

**Các giai đoạn:** backlog → in-progress → review → done (bên lề: blocked)

**Ai làm gì:**

- `backlog → in-progress` (claim): **implementer/tester/architect**
- `in-progress → review` (handoff): **implementer/tester** — qua gate: tests-pass, lint-clean, handoff-note
- `review → done` (done): **reviewer** — qua gate: dod-met, no-self-review, human-merge
- `review → in-progress` (review): **reviewer**
- `* → blocked` (block): **bất kỳ ai**
- `blocked → in-progress` (unblock): **bất kỳ ai**

**Nghi thức (mở thread định kỳ):** planning (manual), retro (end-of-sprint), standup-digest (daily).

## Chat protocol (giao tiếp = backlog) — Hatch MCP

Bạn nối với cả squad qua MCP server **`hatch`** (đăng ký sẵn cho agent của bạn). Tools: `whoami`, `chat_open`, `chat_post`, `chat_read`, `chat_inbox`, `chat_search`, `chat_channels`, `kb_add`, `kb_search`.

Cư xử như một thành viên squad người:

- **Đầu session:** `whoami` rồi `chat_inbox` để "đọc phòng" (DM/@mention/broadcast mới).
- **Mỗi task = một thread:** `chat_open(title, body)`. Brief tiến độ & kết quả bằng `chat_post(reply_to=<id gốc>)` — thread chính là task.
- **Cần đồng đội:** `@tag` đúng agent/role trong body (hoặc `to=`). Ai được tag sẽ đọc thread rồi trả lời **trong cùng thread**.
- **Việc chung:** mở topic và `@all` (hoặc `to=*`).
- **Trước khi suy diễn lại:** `chat_search` / `kb_search`. Recall theo từ khoá, đừng đọc hết.
- **Tri thức đáng giữ:** `kb_add` (type=decision|domain|learning).
- **Báo trạng thái** ngay trong thread: post kết quả khi xong, nêu lý do khi block.

## Definition of Done (tự kiểm trước khi báo xong)

Hatch không chạy gate thay bạn — **bạn tự chạy & xác nhận** trước khi post kết quả:

- [ ] `make lint` xanh (lint-clean).
- [ ] `make test` xanh (tests-pass).
- [ ] **Không tự review** code mình viết — @tag một reviewer khác.
- [ ] **Không tự merge** — người cuối cùng merge là con người.
- [ ] Ghi quyết định/bài học đáng giữ bằng `kb_add`.
- [ ] Post kết quả (+ lý do nếu block) vào thread của task.

## Context map (L2 — đọc on-demand, KHÔNG nạp sẵn)

Khi nhận một task (thread), đọc thread + `context_refs` liên quan dưới đây. Tra Knowledge Base (`kb_search`) trước khi suy diễn lại.

- `.hatch/context/tech`
- `.hatch/context/product`

- Knowledge Base: tool `kb_search` (hoặc `.hatch/kb/index.md`) — quyết định & bài học chung.
- SSOT: `.hatch/{charter.md,roles/,registry.yaml,workflow.yaml}` — sửa rồi chạy `hatch compile`.

