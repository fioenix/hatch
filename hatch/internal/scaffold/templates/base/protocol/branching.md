# Branching

- **Branch-per-ticket:** `hatch/T-<id>-<slug>`.
- Một chuyển trạng thái = một commit (cùng commit với `git mv` + entry ledger).
- PR cho mỗi ticket; merge chỉ sau human gate.
- Mặc định branch-per-ticket; đổi sang trunk-based bằng cách sửa file này + workflow.
