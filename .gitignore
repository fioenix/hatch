.DS_Store
node_modules/
*.pyc
__pycache__/
promo/

# Hatch dogfood workspace for this repo (local-only): the .hatch SSOT plus the
# compiled surfaces + MCP configs that `hatch init` generates at the repo root.
/.hatch/
/.kiro/
/.mcp.json
/CLAUDE.md
/AGENTS.md
/GEMINI.md
